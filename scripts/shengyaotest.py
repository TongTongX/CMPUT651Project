import utils as utils

import torch
from torchtext import data
from torchtext import datasets
import random
import numpy as np

if __name__ == "__main__":
    # dataset, batch_num = utils.readData('trial',batch_size=1024)
    # # read all data
    # text,y = utils.getBatchSample(dataset,0,'texty')
    
    